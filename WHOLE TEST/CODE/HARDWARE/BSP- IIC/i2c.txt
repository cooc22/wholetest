/*
i2c接口：eepROM:24LC01B/AT24C2051/AT24C01-02
時序為：SCL&SDA可以通過上拉電阻鉗位在高電平，SDA上面的數據僅在SCL為低電平週期時候才能改變；當SCL為高
電平時SDA的改變只能是起始位和終止位。此時所有地址和數據都以8位串行碼的方式輸入輸出eepROM。
數據傳送：i2c總線的數據傳送是從開始信號之後，送出的第一個字節數是用來選擇從器件的地址的，其中前7位為
地址碼，第8位為方向位（r/w）“0”發送“1”接收 開始信號後系統的各器件將自己的地址和主器件送到總線上的地址
進行比較，如果地址一致，擇該期間即為被主器件尋址的器件，接收或發送由第8為來決定。
/*
在i2c總線上每次傳送的字節數不限制，但每一字節必須為8位，而且每一個傳送的字節後面必須更一個ACK.
*/
/*在I2C总线的应用中应注意的事项总结为以下几点:
 1） 严格按照时序图的要求进行操作，
 2） 若与口线上带内部上拉电阻的单片机接口连接，可以不外加上拉电阻。
 3） 程序中为配合相应的传输速率，在对口线操作的指令后可用NOP指令加一定的延时。
 4） 为了减少意外的干扰信号将EEPROM内的数据改写可用外部写保护引脚（如果有），
 或者在EEPROM内部没有用的空间写入标志字，每次上电时或复位时做一次检测，判断EEPROM是否被意外改写。
*/
/*
使用示波器抓取信號必須同時採集SDA&SCL的數據，該如何抓取呢？將示波器調成下降沿觸發模式（SDA由1to 0）
而且設置成單次模式這樣抓取完一次就會stop便於數據分析
*/
/*
總線的競爭仲裁
*/
